"""Category subpackage — L2/L1 hierarchy registry, presence resolver, and
admin override engine. The registry is the single source of truth for role
tags (is-salary / is-recurring / L1 bucketing); see ``registry.py``.
"""
